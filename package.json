{
  "name": "wrapsequel",
  "version": "1.0.4",
  "description": "A MySQL wrapper that allows you to perform basic CRUD updates on a DB without writing any SQL. Results are returned as promises allowing the uses of 'await' when synchronous request are required.",
  "main": "wrapsql.js",
  "scripts": {
    "test": "mocha test --exit"
  },
  "keywords": [
    "nodejs",
    "mysql",
    "pdo",
    "wrapper"
  ],
  "author": "Tracey Boydston",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^8.2.1"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "mysql": "^2.18.1"
  }
}
